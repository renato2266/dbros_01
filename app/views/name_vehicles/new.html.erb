<h1>New Join Name Vehicle</h1>

<% form_for(@name_vehicle) do |f| %>
  <%= f.error_messages %>


	<p>
      <%= f.label :name_id %><br />
      <%= f.collection_select :name_id, @names, :id, :cognome_nome, :include_blank=>true%>
    </p>

	<p>
      <%= f.label :vehicle_id %><br />
      <%= f.collection_select :vehicle_id, @vehicles, :id, :targa, :include_blank=>true%>
    </p>


  <p>
    <%= f.label :tipo_relazione %><br />
    <%= f.select :tipo_relazione, ["in uso", "proprietà" , "passeggero"], :include_blank=>true %>
  </p>

<p>
	<%= f.label :note %><br />
    <%= f.text_area :note %>
</p>

  <p>
    <%= f.label :inizio_relazione %><br />
    <%= f.date_select :inizio_relazione, :include_blank=>true %>
  </p>
  <p>
    <%= f.label :fine_relazione %><br />
    <%= f.date_select :fine_relazione, :include_blank=>true %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', name_vehicles_path %>